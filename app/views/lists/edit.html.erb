<div class="container">

  <h2>Add a list</h2>

  <%= simple_form_for [@trip, @list] do |f| %>
    <%= f.input :title %>

    <%= f.button :submit, "Edit", class: "btn btn-primary" %>

  <% end %>

  <%= link_to "< Back", trip_path(@list.trip), class: "btn btn-secondary"  %>
</div>

